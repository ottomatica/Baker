---
- hosts: all
  become: yes

  tasks:
    {{#env}}
    - name: Adding the path in the bashrc files
      lineinfile:
        dest: /etc/environment
        line: 'export {{KEY}}={{VALUE}}'
        insertafter: 'EOF'
        state: present

    {{/env}}
