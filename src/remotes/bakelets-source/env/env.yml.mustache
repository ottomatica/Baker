---
- hosts: all
  become: yes

  tasks:
    {{#env}}
    - name: Adding the path in the bashrc files
      lineinfile:
        dest: /home/{{=<% %>=}}{{ansible_user}}<%={{ }}=%>/.bashrc
        line: 'export {{KEY}}={{VALUE}}'
        insertafter: 'EOF'
        state: present

    {{/env}}
